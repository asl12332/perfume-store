generator client {
    provider = "prisma-client-js"
    output   = "../src/generated/prisma"
}

datasource db {
    provider = "postgresql"
    url = "postgresql://postgres:FpNAMPebwoxTABHEHmsPIdBFVNGmdhvu@trolley.proxy.rlwy.net:39949/railway"
}

model Product {
    id        Int      @id @default(autoincrement())
    name      String
    price     Float
    image     String?
    createdAt DateTime @default(now())
}

model User {
    id       Int    @id @default(autoincrement())
    email    String @unique
    password String
    name     String?
}